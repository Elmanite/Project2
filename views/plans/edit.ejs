<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title></title>
	</head>
	<body>
		<header>
			<h1>Edit <%=plans.planName%>'s Info</h1>
			<nav>
				<ul>
					<li>
						<a href="/">Home</a>
					</li>
					<li>
						<a href="/plans">Financial plans Index</a>
					</li>
				</ul>
			</nav>
		</header>
		<main>
			<h2>Financial Plan Attributes:</h2>
			<form action="/plans/<%=plans._id%>?_method=PUT" method="post">
				<select name="customerId">
                    <% for(let i = 0; i < customers.length; i++) { %>
                        <option
							value="<%=customers[i]._id%>"
							<% if(customers[i]._id.toString() === planCustomer._id.toString()){ %>
								selected
							<% } %>
							>
							<%=customers[i].firstName%> <%=customers[i].lastName%>
						</option>
                    <% } %>
                </select><br/>
				<input type="text" name="planName" value="<%=plans.planName%>"/><br/>
				<input type="text" name="expenses.expenseCategory" value="<%=plans.expenses.expenseCategory%>"/><br/>
				<input type="submit" value="Update Financial Plan"/>
			</form>
		</main>
	</body>
</html>
