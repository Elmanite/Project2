<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title></title>
	</head>
	<body>
		<header>
			<h1>Edit <%=plan.planName%>'s Info</h1>
			<nav>
				<ul>
					<li>
						<a href="/">Home</a>
					</li>
					<li>
						<a href="/plans">Financial plans Index</a>
					</li>
				</ul>
			</nav>
		</header>
		<main>
			<h2>Financial Plan Attributes:</h2>
			<form action="/plans/<%=plan._id%>?_method=PUT" method="post">
				<select name="UserId">
                    <% for(let i = 0; i < users.length; i++) { %>
                        <option
							value="<%=users[i]._id%>"
							<% if(users[i]._id.toString() === planUser._id.toString()){ %>
								selected
							<% } %>
							>
							<%=users[i].name%>
						</option>
                    <% } %>
                </select><br/>
				<input type="text" name="title" value="<%=plan.planName%>"/><br/>
				<textarea name="body"><%=plan.body%></textarea><br/>
				<input type="submit" value="Update Financial Plan"/>
			</form>
		</main>
	</body>
</html>
